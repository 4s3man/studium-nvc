{% extends "page.twig" %}

{% block content %}
	<div class="content-wrapper">
        {{ slider }}
        {{ idea }}
        <hr>
        {{ logistic }}
        <hr>
        {{ harmonogram }}
        <hr>
        {{ coachs }}
        <hr>
        {{ singups|wpautop }}
        <hr>
        {{ newsletter }}
        {{ contact }}
	</div>
{% endblock %}